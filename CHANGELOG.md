# Changelog

本项目使用接近 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的格式记录版本变化。

## [0.1.1] - 2026-07-13

### Fixed

- 改为本地 JSON 优先的持久化流程，AstrBot KV 用于镜像和本地文件缺失时的恢复。
- 防止在进行中的会话上重复执行 `/trpg_start` 导致跑团数据被覆盖。
- 限制非成员执行 `/trpg_act`、`/trpg_end` 和 `/trpg_export`。
- 使 `/trpg_end` 在成功时立即删除当前会话记录，并在删除失败时保留数据。
- 拒绝规则状态补丁中的负数伤害、负数治疗和无效资源/技能变化值。
- WebGUI 配置接口在收到无效整数时返回 `400`，不再产生未处理异常。

### Changed

- 将 AstrBot 兼容层、存储、事件工具、展示、剧本 I/O、Web 管理、会话命令和行动命令拆分为独立模块。
- 开发依赖现在通过 `requirements-dev.txt` 包含运行时依赖。
- 声明 AstrBot 兼容范围为 `>=4.26,<5`。

## [0.1.0] - 2026-07-09

### Added

- 初始发布 LLM TRPG/跑团插件。
- 支持跑团启动、玩家加入、角色预设、角色卡、状态查看和行动顺序。
- 支持 GM/KP 叙事、结构化骰子请求、状态变更白名单和二次结算叙事。
- 支持基础骰子表达式和 GIF 掷骰输出。
- 支持战役记忆、线索、实体、事实、剧情线、关系和时间线。
- 支持 Markdown 日志导出。
- 支持 AstrBot 插件 WebGUI 管理剧本、配置和战役知识库。
- 支持剧本级行动顺序模式，可在 `llm_gm` LLM 主持人强控制和 `soft` 玩家软顺序之间切换。
- 新增 256x256 插件 Logo。
- 补充 README 中的支持平台说明。
- 补充发布所需的 README、许可证和归档规则。
